# 变更说明

## Unreleased — 2.3.0-dev

1. GitHub 仓库已更名为 sumulige-coding-agent-workflow，工作流分支已合入 main；保留 v2.2 历史，尚未发布 release。
2. 新增 core/web/registry 模板、安装锁、升级预览、冲突拒绝、备份与回退。
3. 新增任务 JSON、证据检查、命令留档与 TODO 派生；区分任务、测试、审查和发布。
4. 七客户端接入与全局入口预览；Cursor/Hermes 保留人工整合边界。
5. v2.2 配置与旧安装器兼容；临时项目回归不代替真实客户端或独立审查。

## v2.2 接入补充 — 2026-09-19

按使用者提供的客户端组合，补充 Cursor、Pi Coding Agent、Hermes Agent、Codex 接入。
仍属同一 v2.2 PR，不升级配置 schema、不改目标项目命令或放宽权限。

- 新增 docs/agent/clients.md：一个 AGENTS.md 入口，分别解释覆盖规则、运行模式、权限与交接。
- 特别说明 Pi 项目信任不是逐命令审批；Hermes 专用入口可能遮蔽通用入口，SOUL/个人记忆不是项目治理。
- 核对官方来源；本地客户端具体版本、模型与原生行为未知，不以网页支持代替运行验收。
- 将接入分册加入核心文件清单：跟随只创建安装器交付并纳入静态校验，不生成四套运行配置。
- 新增三项安装/结构回归：指南交付、保留客户端既有文件、不创建覆盖规则或运行设置。
- 增加八条未执行的真实客户端场景；不宣称这些场景或独立审查已经通过。

## v2.2 — 2026-09-19

基于 v2.1 提交 9b4f6c89c054fd92770c0f6b1dc0978d9fc495aa。
本次是用户明确授权的治理维护，包含根规则、配置、验证代码、模板和 CI；不修改目标产品或仓库权限。
范围复核：本次超过 300 行，因配置迁移、校验器、配套规则与回归测试需要保持一致，作为一个可审查的迁移 PR 交付。

| v2.1 问题 | v2.2 改进 | 验证/边界 |
|---|---|---|
| 用正则截取 YAML 状态前缀 | 唯一 project.json，严格解析与字段校验 | 拒绝 configured-invalid、重复键、非法类型、非 JSON 数值 |
| 配置只看字段是否存在 | 模板完整性与静态就绪模式分开 | configured 空命令失败；不执行命令、不宣称运行就绪 |
| 规则仅检查 HR 注释标记 | 标记唯一且对应正文非空，核心文档非空 | 反例测试覆盖；不是语义等价证明 |
| 测试 GREEN 等于全部完成 | 任务 COMPLETE/PARTIAL/BLOCKED 与测试五态分开 | lint/必需验收失败不能 COMPLETE；真实 Agent 行为待验收 |
| 所有任务走同一流程 | 只读、规格、维护、功能、Bug、治理任务分级 | 只读不写记忆；明确授权范围可连续执行 |
| 全局只读与初始化互相阻塞 | 显式授权的配置/规则维护例外 | 不可自授产品实现、秘密访问或部署权限 |
| 任意文件可读，只禁止提交秘密 | 最小读取与秘密全生命周期约束 | 必须另配权限/沙箱，不声称文本可以强制隔离 |
| 一键复制整个包覆盖现有项目 | 安装默认预览、冲突前置、排他创建 | 不覆盖 README/配置，不部署 CI 或预存结果 |
| 自检无条件落盘 | 标准输出，JSON 带输入指纹 | 删除陈旧 results.json；反例验证默认无写入 |
| 300 行和三次失败机械中断 | 范围复核与无新证据止损 | 不用通用阈值代替风险判断 |
| 自动写共享 memory | 默认只给建议；授权追加事实 | 明确并行隔离、交接与事实/推断区分 |
| 缺工具接入与执行记录 | CLAUDE.md 导入、官方来源、真实场景清单 | 未声称运行所有厂商客户端 |
| 无 CI | 最小权限、固定 SHA、双 Python 版本的 CI | 不自动合并、部署或修改分支保护 |

### 迁移影响

旧 project.md 中的 YAML 不再被解析；手动迁入 docs/agent/project.json，详见 docs/agent/project.md。
新命令 --mode bundle 默认检查模板结构；--mode ready 检查静态配置就绪。
本包继续默认 unconfigured，不提供自动执行任意配置命令的功能。
保留原需求/设计/ADR 模板；更新 tasks 与 memory 的状态和授权格式。

### 证据诚实性

本仓库测试只覆盖检查器/安装器的软件行为；真实 Agent、工具权限、原生宿主与产品验收分开记录。
实现者自检不是独立审查。网页事实来源与项目设计选择在 docs/agent/tooling.md 中分开标注。
