# 项目自有规则

状态：DRAFT；由项目维护者填写实际约定。安装器只初始化本文件，升级保留本地内容。

记录项目特有的编码、目录、协作约定，并引用已有事实来源；真实 lint/test 命令仍维护于 docs/agent/project.json。
本文件不改变运行平台的指令优先级、用户授权、保护范围或必需验收。
若已有定制写在 managed 规则中，先审阅差异并移到此处，核对旧安装指纹后恢复共享基线，再预览升级。
不要使用 AGENTS.override.md 隐藏共享入口，也不要为消除冲突强制覆盖未审阅内容。
