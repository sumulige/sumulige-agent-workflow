# 开发与分发

1. 从明确候选建立工作分支，保留已有改动。
2. 规则包维护适用 AGENTS 的显式授权例外；project.json 保持 unconfigured，不伪造产品配置。
3. CLI/JSON 变化补充反例与维护说明，保留 v2.2 命令兼容。
4. 使用 unittest 和 TemporaryDirectory，不测试真实 home 或客户项目。
5. 代码与相关文档同改；模板与本分发项目事实分开。
6. 按 TESTING.md 验证，再由独立执行者审查确定候选。
7. 发布前确认远端名称、分支、版本和授权；2.3.0-dev 不代表已发布。

本次超过 300 行，因模板、契约、安装与回归是同一交付面；按模块保持内聚，不为行数拆坏契约。
