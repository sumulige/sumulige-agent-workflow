# 自检与剩余验收

1. 实现者：primary-agent；本任务未调用子 Agent。本文为自检，不是独立审查。
2. 检查范围：新 CLI 契约、模板归属、升级与回退、任务状态、全局入口差异、旧安装器兼容。
3. 自检修复：命令不可用从 RED 改为 UNKNOWN；零测试从 UNKNOWN 改为 UNTESTED；备份目录内自动创建 .gitignore，避免在目标项目中误提交旧内容。
4. 故障证据：模拟 OS 文件替换故障后可回退已完成的部分，保留无关内容；篡改备份或后续编辑会拒绝回退。
5. 本地兼容：Python 3.10 语法解析；实际执行版本写入 checks.log。跨 Python 的远端 CI 待新候选运行。
6. 原项目：sumulige-claude 的 git status 与先前快照一致，HEAD 保持 79cdda5b18f93c7eb0f006702e1fdf04f094ac14；没有对其执行写入命令。
7. 未验证：七客户端真正加载/遵守规则、跨客户端中断接力、模型成本、独立审查、远端 CI、发布。
8. 限制：文件/日志 hash 不认证身份或语义；命令计数由日志人工核实；事务不保证批次原子性，要求无并发编辑。备份被 Git 忽略，但仍应按项目数据政策保管。
9. GitHub CLI 报告登录令牌无效，但原生 Git 的现有认证可用；12051bbc667ff912b810e7436dd0e5a4fc15497a 已推送到 codex/coding-agent-workflow，并通过 ls-remote 回读一致。未使用连接器替代发布，未改名、合并或发布 release。
10. 首次推送未触发 CI（旧配置只监听 main 或 PR）。本次将 codex/** 分支纳入既有检查，让候选分支推送即可执行同样的只读校验和临时项目测试。
