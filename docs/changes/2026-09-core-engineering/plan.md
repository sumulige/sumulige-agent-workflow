# 核心工程原则整合

1. 目标：保留用户截图的十条结构，将复杂任务方案要求融入第 1、8、9 条，形成规则包中的完整核心工程原则。
2. 授权：用户要求“优雅的并入我的核心工程原则”；已展示第 1、8、9 条替换稿并说明其余七条沿用原文，用户回复“可以”。本轮落盘该确认文本。
3. 背景：当前仓库没有同名原则章节；根 AGENTS.md 已由现有安装器作为 managed 文件分发，可直接承载原则，无需新增分发文件或修改工具契约。
4. 范围：AGENTS.md 中的核心工程原则、README.md 的入口说明、CHANGELOG.md 及本目录的方案和验证记录。
5. 非目标：不改变工具实现、依赖、机器配置、测试实现或产品；不扩展到全局配置、Coach OS 同步、提交推送或分支删除。

## 实施与验收

1. 保留十条及原顺序：第 1 条建立共识和同步背景，第 8 条约束执行和持续留痕，第 9 条以证据判断完成；第 2–7、10 条按截图原文录入。
2. 第 2 条保留用户截图的单文件 500 行上限；该上限严于现有全局 800 行上限，本任务不批量重构既有文件。
3. 原则指导已授权范围内的设计与实现；不授予多租户实现、接口或数据迁移、依赖安装、删除既有成果等额外权限。既有只读和人类确认边界保持。
4. README 引用根入口的唯一正文，CHANGELOG 记录本次整合；不复制另一套规则正文。
5. 完成编辑后检查十条顺序、三个已批准段落和七条截图原文；运行规则包结构、现有回归、任务/TODO、差异检查，保存完整命令与结果。
6. 这是文档维护；结构和回归结果不证明真实 Agent 已遵循原则，原生客户端行为及独立审查不作为已验证结果。
7. 如发生规则含义变化或同一区域出现外部修改，先保留现状再核实；恢复仅针对本次明确的文档差异。

## 进展

1. 已确认 main 工作区干净，复用现有 AGENTS.md 分发。
2. 十条正文已写入根 AGENTS.md，README 增加单一入口，CHANGELOG 记录整合；第 1、8、9 条与确认稿一致，第 2–7、10 条已对照截图核对。
3. 五项本地命令均退出 0，现有回归 87 项通过；完整命令、工作目录、输出和正文差异指纹见 [checks.json](checks.json)。

## 本地交付

```text
TASK_STATUS: COMPLETE
CHANGED: AGENTS.md、README.md、CHANGELOG.md；本目录方案与验证记录
CHECKS: checks.json 中 5 项命令全部退出 0，回归执行并通过 87 项
TEST_STATUS: GREEN
EVIDENCE: checks.json 的 base_head 与 content_diff_sha256；原生 Agent 行为未验证
AUTHORIZATION: 用户确认三条替换稿及其余七条沿用原文；仅本地文档整合
ACTORS: 实现与自检为当前 Codex 主会话；具体模型版本 unknown；无独立审查者
GIT: main，基线 f11e39a0d2b0539cc64a3d2004784d739ffab543；本轮未提交
NEXT: none（本地文档整合范围）
```
